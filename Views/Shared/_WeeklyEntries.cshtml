@model IReadOnlyList<FoodCalendar.Models.FoodEntry>

@if (Model.Count == 0)
{
    <p class="empty-state">まだ今週の登録はありません。</p>
}
else
{
    <table class="entries-table">
        <thead>
            <tr>
                <th>日付</th>
                <th>食べ物名</th>
                <th>量</th>
                <th>カロリー</th>
                <th>比較</th>
                <th>登録日時 (UTC)</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var entry in Model)
        {
            <tr>
                <td>@entry.EntryDate.ToString("yyyy-MM-dd")</td>
                <td>@entry.FoodName</td>
                <td>@entry.Amount</td>
                <td>@entry.Calories kcal / 目標 @entry.TargetCalories</td>
                <td>@entry.CalorieTrendSymbol</td>
                <td>@entry.CreatedAt.ToString("yyyy-MM-dd HH:mm")</td>
            </tr>
        }
        </tbody>
    </table>
}
