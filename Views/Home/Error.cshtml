@model FoodCalendar.Models.ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<section class="card">
    <h2>Unexpected error</h2>
    <p>要求の処理中にエラーが発生しました。</p>
    @if (Model.ShowRequestId)
    {
        <p><strong>Request ID:</strong> <code>@Model.RequestId</code></p>
    }
</section>
